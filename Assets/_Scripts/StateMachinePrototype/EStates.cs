using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Kaizerwald
{
    public enum EStates
    {
        None = -1,
        Idle = 0,
        Move = 1,
        Fire = 2,
        Chase = 3,
        Melee = 4,
    }
}

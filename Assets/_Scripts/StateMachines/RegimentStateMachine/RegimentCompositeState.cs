using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace KaizerWald
{
    /*
    public class RegimentCompositeState : RegimentState
    {
        protected Dictionary<EStates, RegimentState> StatesComposition { get; private set; }
        protected RegimentCompositeState(RegimentStateMachine regimentStateMachine, List<RegimentState> states, EStates stateIdentity) : base(regimentStateMachine, stateIdentity)
        {
            StatesComposition = new Dictionary<EStates, RegimentState>(states.Count);
            states.ForEach(state => StatesComposition.TryAdd(state.StateIdentity, state));
        }
    }
    */
}
